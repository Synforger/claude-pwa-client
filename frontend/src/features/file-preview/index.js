// features/file-preview 配線 entry。 W2 Phase F-file-preview 段階: 物理移送 + import path 修正のみ。
// v2 overlayRegistry 経由は後続 commit で。

import './FilePreviewModal.jsx'

// TODO: 後続 commit で
//   import { register as registerOverlay } from '../../registry/overlayRegistry.js'
//   registerOverlay('previewPath', { dispatch: ({ type, payload }) => { ... } })
